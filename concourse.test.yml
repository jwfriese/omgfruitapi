platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: '1.6'

run:
  path: go/src/github.com/jwfriese/omgfruitapi/test.sh

inputs:
- name: omgfruitapi

outputs:
- name: new-release

